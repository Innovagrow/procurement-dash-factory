# Οδηγίες για Windows

## Ο γρήγορος δρόμος

1. **Κατεβάστε τον κώδικα.** Στο GitHub, στο branch
   `claude/greek-brokers-investment-outreach-gr5j9p`, πατήστε
   **Code → Download ZIP**. Κάντε εξαγωγή σε έναν φάκελο, π.χ.
   `C:\`.

2. **Εγκαταστήστε Python**, αν δεν το έχετε: <https://www.python.org/downloads/>
   Στον installer, **τσεκάρετε «Add Python to PATH»**. Είναι το μόνο σημείο
   που μπερδεύει.

3. **Διπλό κλικ στο `SAROSI.bat`.**

Αυτό είναι όλο. Το αρχείο ελέγχει τι λείπει, το εγκαθιστά, τρέχει τη σάρωση και
ανοίγει τη σελίδα αποτελεσμάτων στον browser σας.

Θα σας ρωτήσει τι θέλετε να αναλύσετε:

| Επιλογή | Τι κάνει |
|---|---|
| **1** | Το δικό σας αρχείο `akinita.csv` — δουλεύει **σήμερα**, δευτερόλεπτα |
| **2** | Το ίδιο, με δικό σας ανώτατο τίμημα |
| **3** | Spitogatos — σάρωση της πύλης· θα σας ρωτήσει σε ποια βάση |
| **4** | Χάρτης «πού να ψάξω» — ανοιχτά δεδομένα, χωρίς καμία αγγελία |

## Τι ισχύει σήμερα με τη σάρωση της πύλης

Έλεγχος 23/08/2026, με πραγματικό browser: η πύλη σερβίρει ένα κέλυφος λίγων
χιλιάδων bytes και φορτώνει **hCaptcha**. Δηλαδή απαντά σε αυτοματοποιημένες
συνεδρίες με πρόκληση, όχι με αποτελέσματα. Αυτό δεν παρακάμπτεται και δεν
επιχειρείται εδώ: το εργαλείο το αναγνωρίζει και το λέει, αντί να επιστρέφει
σιωπηλά «μηδέν ευκαιρίες».

Διορθώθηκε πάντως και ένα δικό μας λάθος που το έκρυβε: οι διαδρομές που
ζητούσε ο κώδικας (`/pwliseis-katoikies/ellada` και τα αδέρφια της) δεν
υπάρχουν πια — επέστρεφαν **404**. Τώρα χρησιμοποιεί τις τρέχουσες (`/sale/…`).

**Ο δρόμος που δουλεύει σίγουρα: η επιλογή 1.** Βάζετε στο `akinita.csv` τα
ακίνητα που ήδη κοιτάτε — από αγγελίες που σας έστειλαν μεσίτες, από δικές σας
αναζητήσεις — και ο μηχανισμός τα περνά από κάθε τρόπο αξιοποίησης και τα
κατατάσσει. Η ανάλυση δεν εξαρτάται από την πύλη· μόνο η αυτόματη συλλογή.

## Η επιλογή 3 και οι Όροι του Spitogatos

Το άρθρο 2 των Όρων τους ορίζει **δύο διαφορετικά πράγματα**:

> *«Users are allowed to print and electronically save the content of the website
> **for personal use**, but **by no means for commercial use**»* — και κάθε
> αντιγραφή ή αναπαραγωγή απαιτεί *«prior consent of the owner»*.

Όταν διαλέξετε 3, το πρόγραμμα ρωτά ποια βάση ισχύει:

| | |
|---|---|
| **P** | Προσωπική, μη εμπορική χρήση — **ρητά επιτρεπτή** από τους Όρους |
| **W** | Έχετε γραπτή άδεια από το Spitogatos (<info@spitogatos.gr>) |

Σε **κάθε** περίπτωση, η **αναδημοσίευση και διανομή απαγορεύεται**: κρατήστε τη
σελίδα αποτελεσμάτων στο μηχάνημά σας, μην τη δημοσιεύσετε και μην τη
διανείμετε. Το πρόγραμμα το υπενθυμίζει σε κάθε εκτέλεση και το γράφει στο
υποσέλιδο της σελίδας.

Όταν δηλώνετε προσωπική χρήση, η έξοδος **δεν φέρει επωνυμία εταιρείας** — θα
ήταν αντίφαση μέσα στο ίδιο μας το αρχείο.

Το **xe.gr** και το **remax.gr** είναι διαφορετική περίπτωση: μας απαγορεύουν
ονομαστικά στο `robots.txt` τους, οπότε δεν τρέχουν καθόλου.

## Μία σελίδα, με καρτέλες

Ό,τι κι αν τρέξετε, βγαίνει **μία** σελίδα:

| Καρτέλα | Τι δείχνει |
|---|---|
| **Ευκαιρίες** | Τα ακίνητα, κατά σειρά απόδοσης — αυτό που ανοίγει πρώτο |
| **Πού να ψάξεις** | Περιοχές κατά ζήτηση και δημόσια έργα, από ανοιχτά δεδομένα |
| **Πώς διαβάζεται** | Τι σημαίνει κάθε αριθμός |

Η επιλογή **4** φτιάχνει τη σελίδα χωρίς καμία αγγελία: μόνο την καρτέλα των
περιοχών, από Eurostat και Διαύγεια. Χρήσιμη για να δείτε πού να ψάξετε πριν
ψάξετε. Χωριστά, από γραμμή εντολών:

```
py -3 -m akinita.ethniki --out out\index.html
```

## Όλα με μία εντολή, από το μηδέν

Αν δεν έχετε καθόλου τον κώδικα στο μηχάνημα, αυτό τον κατεβάζει, εγκαθιστά ό,τι
χρειάζεται και τρέχει τη σάρωση. Επικολλήστε το σε **cmd** — το `curl` και το
`tar` υπάρχουν ήδη σε Windows 10 και 11:

```
cd /d "%USERPROFILE%\Desktop" && curl -L -o akinita.zip "https://github.com/Innovagrow/procurement-dash-factory/archive/refs/heads/claude/greek-brokers-investment-outreach-gr5j9p.zip" && tar -xf akinita.zip && cd procurement-dash-factory-claude-greek-brokers-investment-outreach-gr5j9p && py -3 -m pip install -r requirements-akinita.txt && py -3 -m playwright install chromium && py -3 -m akinita.screener --source spitogatos --all-types --personal-use --max-price 50000 --enrich-top 150 --top 400 --delay 2.5 --out out\eukairies --html-out out\apotelesmata.html && start out\apotelesmata.html
```

Το `ModuleNotFoundError: No module named 'akinita'` σημαίνει ένα πράγμα μόνο:
τρέχετε από φάκελο που δεν περιέχει τον κώδικα. Η παραπάνω εντολή ξεκινά με
`cd` ακριβώς γι' αυτό.

## Ο δρόμος που δουλεύει σήμερα

Επιλογή **1**. Φτιάξτε ένα `akinita.csv` δίπλα στο `SAROSI.bat` — από Excel,
**Αρχείο → Αποθήκευση ως → CSV UTF-8**. Χρειάζεται μόνο τρεις στήλες:

| Στήλη | Υποχρεωτική | Παράδειγμα |
|---|---|---|
| Τιμή | ναι | `45.000 €` ή `45000` |
| Εμβαδόν | ναι | `61` |
| Περιοχή | ναι | `Θεσσαλονίκη (Ξηροκρήνη)` |
| Κατηγορία | όχι | `Διαμέρισμα`, `Οικόπεδο`, `Κατάστημα`, `Θέση στάθμευσης` |
| Έτος | όχι | `1972` |
| Περιγραφή | όχι | `2ος όροφος, χρήζει ανακαίνισης` |
| **Ενοίκιο** | όχι, αλλά **σημαντική** | `430` — χωρίς αυτήν δεν αποτιμώνται τα πλάνα μίσθωσης |
| Link | όχι | το URL της αγγελίας |

Τα ονόματα των στηλών αναγνωρίζονται χαλαρά: `Τιμή`, `Ζητούμενη τιμή`, `Τίμημα`,
`price` — όλα δουλεύουν. Δείτε το `akinita_deigma.csv` ως πρότυπο.

## Τι θα πάρετε

Στον φάκελο `out\`:

- **`apotelesmata_<ημερομηνία>.html`** — η σελίδα. Ανοίγει μόνη της. Φίλτρα ανά
  τύπο και περιοχή, κατάταξη σε όποιον δείκτη θέλετε, και ανά ακίνητο όλα τα
  πλάνα που εξετάστηκαν και όσα απορρίφθηκαν.
- **`eukairies_<ημερομηνία>.csv`** — ανοίγει σε Excel.
- **`eukairies_<ημερομηνία>.json`** — τα πλήρη δεδομένα.

## Ένα ακίνητο σε βάθος

```
py -3 -m akinita.analyse --from-json out\eukairies_20260822.json --rank 1
```

## Αν κάτι πάει στραβά

| Μήνυμα | Τι σημαίνει |
|---|---|
| `Python was not found` | Δεν τσεκάρατε «Add Python to PATH». Ξαναεγκαταστήστε το Python. |
| `403` / `Forbidden` επανειλημμένα | Η πύλη σας περιορίζει. Περιμένετε μία ώρα και ξανατρέξτε. |
| `δεν μπορεί να χρησιμοποιηθεί εμπορικά` | Δεν δηλώσατε βάση χρήσης. Ξανατρέξτε και απαντήστε P ή W. |
| `Could not install the browser` | Χρειάζεται μόνο για την επιλογή 3. Οι επιλογές 1 και 4 δουλεύουν χωρίς αυτόν. |
| `ModuleNotFoundError: No module named 'akinita'` | Τρέχετε από λάθος φάκελο. Δείτε «Όλα με μία εντολή, από το μηδέν». |
| Παράξενοι χαρακτήρες αντί για ελληνικά | Ενημερώστε με — το έχω αντιμετωπίσει, αλλά κάποιες παλιές κονσόλες αντιστέκονται. |

## Τι **δεν** θα τρέξει, και γιατί

| Πύλη | Τι λέει |
|---|---|
| **Spitogatos** | `robots.txt`: επιτρέπει τις σελίδες αναζήτησης, δεν μας ονομάζει. **Όροι Χρήσης: αποθήκευση μόνο για προσωπική χρήση, «by no means for commercial use».** Χρειάζεται γραπτή άδεια. |
| **xe.gr** | `ClaudeBot`/`Claude-Web`: `Disallow: /`. Το `Claude-User` έχει στενή λίστα που δεν περιλαμβάνει ούτε αποτελέσματα ούτε κατάλογο μεσιτών. |
| **remax.gr** | `User-agent: ClaudeBot` → `Disallow: /` σε όλο τον ιστότοπο. |

Το πρόγραμμα δεν αλλάζει User-Agent για να παρακάμψει τίποτα από αυτά, και
σταματά μόνο του με εξήγηση αντί να το κάνει σιωπηλά.
